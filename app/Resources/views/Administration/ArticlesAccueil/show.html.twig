{% extends 'Administration/AdminLayout.html.twig' %}

{% block content %}

<h2 class="mb-2">Articles d'acceuil</h2>

    <table>
        <thead>
        <tr>
            <th>Titre de l'article</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td>{{ article.titre }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_accueil_article_edit', { 'id': article.id }) }}">
                                <button class="btn-success">éditer</button>
                            </a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_accueil_article_new') }}">
                <button class="btn-primary">Nouveau</button>
            </a>
        </li>
    </ul>
{% endblock %}