{% extends 'Administration/AdminLayout.html.twig' %}

{% block content %}

    <table>
        <thead>
        <tr>
            <th>Nom du Plat</th>
            <th>Origine</th>
            <th>Prix</th>
            <th>Description</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for plat in plats %}
            <tr>
                <td>{{ plat.nom }}</td>
                <td>{{ plat.origine }}</td>
                <td>{{ plat.prix }}</td>
                <td>{{ plat.description }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_carte_plat_edit', { 'id': plat.id }) }}">
                                <button class="btn-success">éditer</button>
                            </a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_carte_plat_new') }}">
                <button class="btn-primary">Nouveau</button>
            </a>
        </li>
    </ul>


{% endblock %}